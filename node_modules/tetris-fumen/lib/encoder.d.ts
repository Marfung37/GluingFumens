import { Field, Operation } from './field';
export interface EncodePage {
    comment?: string;
    operation?: Operation;
    field?: Field;
    flags?: {
        lock?: boolean;
        mirror?: boolean;
        colorize?: boolean;
        rise?: boolean;
    };
}
export declare type EncodePages = EncodePage[];
export declare function encode(pages: EncodePage[]): string;
