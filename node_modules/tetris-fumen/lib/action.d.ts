import { InnerOperation } from './defines';
interface Action {
    piece: InnerOperation;
    rise: boolean;
    mirror: boolean;
    colorize: boolean;
    comment: boolean;
    lock: boolean;
}
export declare const createActionDecoder: (width: number, fieldTop: number, garbageLine: number) => {
    decode: (v: number) => Action;
};
export declare const createActionEncoder: (width: number, fieldTop: number, garbageLine: number) => {
    encode: (action: Action) => number;
};
export {};
